name: innertube_dart
description: YouTube stream urls via the InnerTube player endpoint.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.0.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
